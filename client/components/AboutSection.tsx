import React from 'react';

import styles from './AboutSection.module.css';

const AboutSection: React.FC = () => {
  return <section className={`${styles.AboutSection}`}>AboutSection</section>;
};

export default AboutSection;

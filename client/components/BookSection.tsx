import React from 'react';

import styles from './BookSection.module.css';

const BookSection: React.FC = () => {
  return <section className={`${styles.BookSection}`}>BookSection</section>;
};

export default BookSection;

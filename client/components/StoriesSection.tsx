import React from 'react';

import styles from './StoriesSection.module.css';

const StoriesSection: React.FC = () => {
  return <section className={`${styles.StoriesSection}`}>StoriesSection</section>;
};

export default StoriesSection;
